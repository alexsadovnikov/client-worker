summary: Получение пользователя по ID
tags:
  - User
parameters:
  - name: user_id
    in: path
    required: true
    schema:
      type: integer
responses:
  200:
    description: Пользователь получен
    content:
      application/json:
        schema:
          $ref: '#/definitions/User'
