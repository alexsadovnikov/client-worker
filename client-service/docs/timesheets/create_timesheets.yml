summary: Создание записи учёта времени
tags:
  - Timesheet
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/definitions/TimesheetCreate'
responses:
  201:
    description: Запись учёта времени создана
    content:
      application/json:
        schema:
          $ref: '#/definitions/Timesheet'
