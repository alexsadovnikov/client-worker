summary: Получение записи учёта времени по ID
tags:
  - Timesheet
parameters:
  - name: timesheet_id
    in: path
    required: true
    schema:
      type: integer
responses:
  200:
    description: Запись учёта времени получена
    content:
      application/json:
        schema:
          $ref: '#/definitions/Timesheet'
