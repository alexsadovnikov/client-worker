summary: Получение аналитического отчёта по ID
tags:
  - AnalyticsReport
parameters:
  - name: report_id
    in: path
    required: true
    schema:
      type: integer
responses:
  200:
    description: Аналитический отчёт получен
    content:
      application/json:
        schema:
          $ref: '#/definitions/AnalyticsReport'
