summary: Получение проекта по ID
tags:
  - Project
parameters:
  - name: project_id
    in: path
    required: true
    schema:
      type: integer
responses:
  200:
    description: Проект получен
    content:
      application/json:
        schema:
          $ref: '#/definitions/Project'
