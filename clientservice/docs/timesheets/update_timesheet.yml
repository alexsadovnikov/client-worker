summary: Обновление записи учёта времени
tags:
  - Timesheet
parameters:
  - name: timesheet_id
    in: path
    required: true
    schema:
      type: integer
requestBody:
  required: true
  content:
    application/json:
      schema:
        $ref: '#/definitions/TimesheetUpdate'
responses:
  200:
    description: Запись учёта времени обновлена
    content:
      application/json:
        schema:
          $ref: '#/definitions/Timesheet'
