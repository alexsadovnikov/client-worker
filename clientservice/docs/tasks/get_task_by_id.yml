summary: Получение задачи по ID
tags:
  - Task
parameters:
  - name: task_id
    in: path
    required: true
    schema:
      type: integer
responses:
  200:
    description: Задача получена
    content:
      application/json:
        schema:
          $ref: '#/definitions/Task'
