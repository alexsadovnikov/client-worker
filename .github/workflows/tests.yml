name: 🧪 Тесты для worker

on:
  pull_request:
    paths:
      - "clientservice/worker/**"
      - ".github/workflows/tests.yml"
  push:
    branches:
      - main

jobs:
  test:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Клонирование репозитория
        uses: actions/checkout@v4

      - name: 🐍 Установка Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r clientservice/worker/requirements.txt
          pip install pytest pytest-cov

      - name: 🧪 Запуск тестов с покрытием
        run: |
          PYTHONPATH=clientservice/worker pytest \
            --cov=app \
            --cov-report=term \
            --cov-report=xml \
            clientservice/worker/tests

      - name: 🚀 Запуск worker, kafka и crm
        run: |
          docker compose -f docker-compose.yml up -d worker kafka crm

      - name: ⏳ Ожидание readiness
        run: |
          echo "Ожидаем инициализацию контейнеров..."
          sleep 10

      - name: 📤 Отправка покрытия в Codecov
        uses: codecov/codecov-action@v4
        with:
          token: ${{ secrets.CODECOV_TOKEN }}
          files: ./coverage.xml
          flags: unittests
          name: codecov-client-worker

      - name: 🔍 Проверка доступности Swagger UI
        run: |
          curl --fail http://localhost:5050/apidocs || exit 1