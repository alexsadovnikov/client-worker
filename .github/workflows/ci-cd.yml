name: 🧪 CI/CD для ClientService

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: 🧾 Checkout репозитория
        uses: actions/checkout@v3

      - name: 🐍 Установка Python
        uses: actions/setup-python@v4
        with:
          python-version: '3.11'

      - name: 📦 Установка зависимостей
        run: |
          python -m pip install --upgrade pip
          pip install -r clientservice/worker/requirements.txt
          pip install -r clientservice/requirements-dev.txt

      - name: 🧪 Прогон тестов с покрытием
        run: |
          pytest --cov=clientservice/worker \
                 --cov-report=term-missing \
                 --cov-report=xml:clientservice/coverage.xml

      - name: 📈 Отправка покрытия в Codecov
        uses: codecov/codecov-action@v3
        with:
          files: clientservice/coverage.xml
          flags: worker
          name: client-worker
          # 🔐 Раскомментируй, если репозиторий private:
          # token: ${{ secrets.CODECOV_TOKEN }}